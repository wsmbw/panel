@echo off
echo ???Ubuntu Panel???...
mkdir data 2>nul
if not exist config.yaml (
    type config.yaml.template > config.yaml
    echo ??????????????)
if exist ubuntu-panel.exe (
    ubuntu-panel.exe --config config.yaml
) else (
    echo ???: ???????????? ubuntu-panel.exe
    pause
)
